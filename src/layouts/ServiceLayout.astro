---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SpineProgress from '../components/SpineProgress.astro';

interface Props {
  title: string;
  description: string;
  schemaType?: string;
  heroImage?: string;
  heroAlt?: string;
}

const { title, description, schemaType = 'Service', heroImage, heroAlt } = Astro.props;

const schema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": `${title} | Frey Clinic of Chiropractic`,
  "description": description,
  "url": `https://www.freychiropractic.com${Astro.url.pathname}`,
  "isPartOf": { "@id": "https://www.freychiropractic.com/#website" },
  "mainEntity": {
    "@type": schemaType,
    "name": title,
    "provider": { "@id": "https://www.freychiropractic.com/#business" },
    "areaServed": {
      "@type": "City",
      "name": "Napoleon",
      "containedInPlace": { "@type": "State", "name": "Ohio" }
    }
  }
};
---

<BaseLayout title={title} description={description} schema={schema}>
  <Header slot="header" />

  <section class={`page-header ${heroImage ? 'page-header--image' : ''}`}>
    {heroImage && (
      <div class="page-header__bg">
        <img src={heroImage} alt={heroAlt || title} width="1400" height="600" />
      </div>
    )}
    <div class="container">
      <h1 class="reveal">{title}</h1>
      <span class="page-header__accent reveal"></span>
    </div>
  </section>

  <section class="section">
    <div class="container" style="max-width:48rem;">
      <div class="reveal">
        <slot />
      </div>
    </div>
  </section>

  <section class="cta-band">
    <div class="container reveal">
      <h2>Take the next step toward better health</h2>
      <p>Schedule your appointment at Frey Clinic of Chiropractic today.</p>
      <a href="/schedule/" class="btn btn--arrow">Schedule an Appointment</a>
    </div>
  </section>

  <Footer slot="footer" />
  <SpineProgress slot="spine" />
</BaseLayout>
